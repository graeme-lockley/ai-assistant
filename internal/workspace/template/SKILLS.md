# SKILLS

Each skill: `skills/<name>/SKILL.md`. Loader uses `context/routing/skills.json`. Run `ai-assistant index` to regenerate cards.

## List

- **gmail** — Read, search, draft, send email via `gog`. `skills/gmail/SKILL.md`
- **whatsapp** — Send WhatsApp or search/sync history via `wacli`. Third-party only. `skills/whatsapp/SKILL.md`

Add skills: create `skills/<name>/SKILL.md`, list here.
