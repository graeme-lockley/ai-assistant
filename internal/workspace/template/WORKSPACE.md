# WORKSPACE

Context loader policy: what's always loaded, what's selected per request. Spec: `context/` (config, indexes, routing, cache).

- **Core** (always): SOUL.md, AGENT.md, IDENTITY.md
- **Supporting** (per request): USER.md, MEMORY.md, TASKS.md fragments
- **Capabilities** (when relevant): skills and tools via routing

Tune: `context/config.yaml`
