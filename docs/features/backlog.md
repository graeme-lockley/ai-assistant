# Feature backlog

Prior TypeScript/Go backlog items were cleared as part of the pi-mono pivot. New work is tracked informally here until numbered specs return.

**Possible next steps**

- Semantic retrieval over `fragments.jsonl`
- SQLite-backed `SessionStorage` implementation
- Swift iOS client against the same HTTP API
- Auth/TLS for multi-user deployments

**Done (baseline)**

- npm monorepo with `core`, `server`, `repl`, `ask`, `consolidate`
- Multi-agent `agents/*/AGENT.md` (Agent Skills–style)
- File-backed persistent sessions
- Memory layout: `memory/daily|weekly|monthly`, `MEMORY.md`, `logs/`
- Seven tools + Tavily search
- Fragment indexer
